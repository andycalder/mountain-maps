<%= turbo_frame_tag("photo-upload") do %>
  <%= simple_form_for(@photo) do |f| %>
    <%= f.input :image, as: :file %>
    <%= f.button :submit, "Upload", class: "btn btn-primary"%>
  <% end %>
<% end %>
